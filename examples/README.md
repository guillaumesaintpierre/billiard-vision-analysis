# Examples

Keep only one or two representative generated outputs here.

Recommended contents for a public example:

- `pos.txt`
- `SummaryT1.txt`
- `ScoreSheetT1.pdf`

Do not commit all runtime `pixmap.bin`, `AnalyseT*.m`, `SummaryT*.txt` and `ScoreSheetT*.pdf` files from every sequence.
